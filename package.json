{
  "name": "tbs-bot",
  "version": "1.0.0",
  "description": "TB Server Bot with strikes, approvals, song requests, and status system",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "deploy": "node deploy-commands.js",
    "smoke": "node scripts/validate-bot.js",
    "check": "node --check index.js && node --check db.js && node --check deploy-commands.js && node --check slashCommands.js && node --check slashCommandRoutes.js && node --check commands/moderation.js && node --check commands/moderationPermissions.js && node --check commands/tickets.js && node --check commands/fortniteQueue.js && node --check commands/applications.js && node scripts/validate-bot.js",
    "audit:prod": "npm audit --omit=dev --audit-level=critical"
  },
  "dependencies": {
    "better-sqlite3": "^9.0.0",
    "discord.js": "^14.16.0",
    "dotenv": "^16.4.0",
    "express": "^5.2.1",
    "node-fetch": "^3.3.2",
    "open": "^11.0.0"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10"
  }
}
